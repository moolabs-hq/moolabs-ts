/* tslint:disable */
/* eslint-disable */
/**
 * Moolabs API
 * Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.
 *
 * The version of the OpenAPI document: 0.3.0-rc2
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import type { Configuration } from '../configuration';
import type { AxiosPromise, AxiosInstance, RawAxiosRequestConfig } from 'axios';
import globalAxios from 'axios';
// Some imports not used depending on template conditions
// @ts-ignore
import { DUMMY_BASE_URL, assertParamExists, setApiKeyToObject, setBasicAuthToObject, setBearerAuthToObject, setOAuthToObject, setSearchParams, serializeDataIfNeeded, toPathString, createRequestFunction } from '../common';
// @ts-ignore
import { BASE_PATH, COLLECTION_FORMATS, type RequestArgs, BaseAPI, RequiredError, operationServerMap } from '../base';
// @ts-ignore
import type { HTTPValidationError } from '../models';
/**
 * WordAddinApi - axios parameter creator
 * @export
 */
export const WordAddinApiAxiosParamCreator = function (configuration?: Configuration) {
    return {
        /**
         * Serve the latest master .docx stamped with a Moolabs bind custom-XML part.  The bind part is a ZIP-embedded XML element (namespace urn:moolabs:bind) whose text content is a JSON object:   {tenant_id, quote_id, doc_key, base_version, bind_token}  The bind_token is a short-TTL (30 min) HS256 JWT scoped to (tenant_id, quote_id, doc_key) with purpose=\"bind\".  It expires so a forwarded copy of the file cannot be replayed after the TTL.  The custom-XML part is NOT visible in Word\'s standard UI but IS readable by any party who unzips the file — ids only, no long-lived secrets.  Returns 404 when no document has been uploaded for this quote. Returns 503 when WORD_ADDIN_JWT_SECRET is not configured.
         * @summary Word Open
         * @param {string} quoteId 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        wordOpen: async (quoteId: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'quoteId' is not null or undefined
            assertParamExists('wordOpen', 'quoteId', quoteId)
            const localVarPath = `/v1/quotes/{quote_id}/contract/editor/word/open`
                .replace(`{${"quote_id"}}`, encodeURIComponent(String(quoteId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'GET', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;

            // authentication HTTPBearer required
            // http bearer authentication required
            await setBearerAuthToObject(localVarHeaderParameter, configuration)


    
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
        /**
         * Accept the add-in\'s current .docx and store it as a new master version.  Auth (SPIKE — see PRODUCTION NOTE in module docstring):   The caller must supply the bind_token in the X-Bind-Token header.   The token is verified (signature + expiry + scope) to prove the caller   holds the file that was served by /word/open.   PRODUCTION: replace bind-token auth with durable session token +   require_quotes_enabled dependency per spec §9.  Version-fence:   base_version must equal the current head version_no for this quote\'s   contract.  If not (someone else pushed a new version, or the doc is   stale) → 409 Conflict.  The add-in must reconcile (re-open the new   master) before pushing again.  Validation:   - File must start with PK\\x03\\x04 (OOXML/ZIP magic).   - File size must be <= 30 MiB.  Returns:   200 {doc_key, version_no, status: \"stored\"} on success.   409 {detail: \"Version conflict...\"} on stale base_version.   422 on invalid bytes.   401/403 on bad/missing token.
         * @summary Word Sync
         * @param {string} quoteId 
         * @param {File} file The current .docx from the add-in
         * @param {number} baseVersion The version_no the add-in derived this doc from
         * @param {string} [xBindToken] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        wordSync: async (quoteId: string, file: File, baseVersion: number, xBindToken?: string, options: RawAxiosRequestConfig = {}): Promise<RequestArgs> => {
            // verify required parameter 'quoteId' is not null or undefined
            assertParamExists('wordSync', 'quoteId', quoteId)
            // verify required parameter 'file' is not null or undefined
            assertParamExists('wordSync', 'file', file)
            // verify required parameter 'baseVersion' is not null or undefined
            assertParamExists('wordSync', 'baseVersion', baseVersion)
            const localVarPath = `/v1/quotes/{quote_id}/contract/editor/word/sync`
                .replace(`{${"quote_id"}}`, encodeURIComponent(String(quoteId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }

            const localVarRequestOptions = { method: 'POST', ...baseOptions, ...options};
            const localVarHeaderParameter = {} as any;
            const localVarQueryParameter = {} as any;
            const localVarFormParams = new ((configuration && configuration.formDataCtor) || FormData)();


            if (file !== undefined) { 
                localVarFormParams.append('file', file as any);
            }
    
            if (baseVersion !== undefined) { 
                localVarFormParams.append('base_version', baseVersion as any);
            }
    
    
            localVarHeaderParameter['Content-Type'] = 'multipart/form-data';
    
            if (xBindToken != null) {
                localVarHeaderParameter['X-Bind-Token'] = String(xBindToken);
            }
            setSearchParams(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = {...localVarHeaderParameter, ...headersFromBaseOptions, ...options.headers};
            localVarRequestOptions.data = localVarFormParams;

            return {
                url: toPathString(localVarUrlObj),
                options: localVarRequestOptions,
            };
        },
    }
};

/**
 * WordAddinApi - functional programming interface
 * @export
 */
export const WordAddinApiFp = function(configuration?: Configuration) {
    const localVarAxiosParamCreator = WordAddinApiAxiosParamCreator(configuration)
    return {
        /**
         * Serve the latest master .docx stamped with a Moolabs bind custom-XML part.  The bind part is a ZIP-embedded XML element (namespace urn:moolabs:bind) whose text content is a JSON object:   {tenant_id, quote_id, doc_key, base_version, bind_token}  The bind_token is a short-TTL (30 min) HS256 JWT scoped to (tenant_id, quote_id, doc_key) with purpose=\"bind\".  It expires so a forwarded copy of the file cannot be replayed after the TTL.  The custom-XML part is NOT visible in Word\'s standard UI but IS readable by any party who unzips the file — ids only, no long-lived secrets.  Returns 404 when no document has been uploaded for this quote. Returns 503 when WORD_ADDIN_JWT_SECRET is not configured.
         * @summary Word Open
         * @param {string} quoteId 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async wordOpen(quoteId: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<any>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.wordOpen(quoteId, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['WordAddinApi.wordOpen']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
        /**
         * Accept the add-in\'s current .docx and store it as a new master version.  Auth (SPIKE — see PRODUCTION NOTE in module docstring):   The caller must supply the bind_token in the X-Bind-Token header.   The token is verified (signature + expiry + scope) to prove the caller   holds the file that was served by /word/open.   PRODUCTION: replace bind-token auth with durable session token +   require_quotes_enabled dependency per spec §9.  Version-fence:   base_version must equal the current head version_no for this quote\'s   contract.  If not (someone else pushed a new version, or the doc is   stale) → 409 Conflict.  The add-in must reconcile (re-open the new   master) before pushing again.  Validation:   - File must start with PK\\x03\\x04 (OOXML/ZIP magic).   - File size must be <= 30 MiB.  Returns:   200 {doc_key, version_no, status: \"stored\"} on success.   409 {detail: \"Version conflict...\"} on stale base_version.   422 on invalid bytes.   401/403 on bad/missing token.
         * @summary Word Sync
         * @param {string} quoteId 
         * @param {File} file The current .docx from the add-in
         * @param {number} baseVersion The version_no the add-in derived this doc from
         * @param {string} [xBindToken] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        async wordSync(quoteId: string, file: File, baseVersion: number, xBindToken?: string, options?: RawAxiosRequestConfig): Promise<(axios?: AxiosInstance, basePath?: string) => AxiosPromise<any>> {
            const localVarAxiosArgs = await localVarAxiosParamCreator.wordSync(quoteId, file, baseVersion, xBindToken, options);
            const localVarOperationServerIndex = configuration?.serverIndex ?? 0;
            const localVarOperationServerBasePath = operationServerMap['WordAddinApi.wordSync']?.[localVarOperationServerIndex]?.url;
            return (axios, basePath) => createRequestFunction(localVarAxiosArgs, globalAxios, BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
        },
    }
};

/**
 * WordAddinApi - factory interface
 * @export
 */
export const WordAddinApiFactory = function (configuration?: Configuration, basePath?: string, axios?: AxiosInstance) {
    const localVarFp = WordAddinApiFp(configuration)
    return {
        /**
         * Serve the latest master .docx stamped with a Moolabs bind custom-XML part.  The bind part is a ZIP-embedded XML element (namespace urn:moolabs:bind) whose text content is a JSON object:   {tenant_id, quote_id, doc_key, base_version, bind_token}  The bind_token is a short-TTL (30 min) HS256 JWT scoped to (tenant_id, quote_id, doc_key) with purpose=\"bind\".  It expires so a forwarded copy of the file cannot be replayed after the TTL.  The custom-XML part is NOT visible in Word\'s standard UI but IS readable by any party who unzips the file — ids only, no long-lived secrets.  Returns 404 when no document has been uploaded for this quote. Returns 503 when WORD_ADDIN_JWT_SECRET is not configured.
         * @summary Word Open
         * @param {string} quoteId 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        wordOpen(quoteId: string, options?: RawAxiosRequestConfig): AxiosPromise<any> {
            return localVarFp.wordOpen(quoteId, options).then((request) => request(axios, basePath));
        },
        /**
         * Accept the add-in\'s current .docx and store it as a new master version.  Auth (SPIKE — see PRODUCTION NOTE in module docstring):   The caller must supply the bind_token in the X-Bind-Token header.   The token is verified (signature + expiry + scope) to prove the caller   holds the file that was served by /word/open.   PRODUCTION: replace bind-token auth with durable session token +   require_quotes_enabled dependency per spec §9.  Version-fence:   base_version must equal the current head version_no for this quote\'s   contract.  If not (someone else pushed a new version, or the doc is   stale) → 409 Conflict.  The add-in must reconcile (re-open the new   master) before pushing again.  Validation:   - File must start with PK\\x03\\x04 (OOXML/ZIP magic).   - File size must be <= 30 MiB.  Returns:   200 {doc_key, version_no, status: \"stored\"} on success.   409 {detail: \"Version conflict...\"} on stale base_version.   422 on invalid bytes.   401/403 on bad/missing token.
         * @summary Word Sync
         * @param {string} quoteId 
         * @param {File} file The current .docx from the add-in
         * @param {number} baseVersion The version_no the add-in derived this doc from
         * @param {string} [xBindToken] 
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        wordSync(quoteId: string, file: File, baseVersion: number, xBindToken?: string, options?: RawAxiosRequestConfig): AxiosPromise<any> {
            return localVarFp.wordSync(quoteId, file, baseVersion, xBindToken, options).then((request) => request(axios, basePath));
        },
    };
};

/**
 * WordAddinApi - interface
 * @export
 * @interface WordAddinApi
 */
export interface WordAddinApiInterface {
    /**
     * Serve the latest master .docx stamped with a Moolabs bind custom-XML part.  The bind part is a ZIP-embedded XML element (namespace urn:moolabs:bind) whose text content is a JSON object:   {tenant_id, quote_id, doc_key, base_version, bind_token}  The bind_token is a short-TTL (30 min) HS256 JWT scoped to (tenant_id, quote_id, doc_key) with purpose=\"bind\".  It expires so a forwarded copy of the file cannot be replayed after the TTL.  The custom-XML part is NOT visible in Word\'s standard UI but IS readable by any party who unzips the file — ids only, no long-lived secrets.  Returns 404 when no document has been uploaded for this quote. Returns 503 when WORD_ADDIN_JWT_SECRET is not configured.
     * @summary Word Open
     * @param {string} quoteId 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WordAddinApiInterface
     */
    wordOpen(quoteId: string, options?: RawAxiosRequestConfig): AxiosPromise<any>;

    /**
     * Accept the add-in\'s current .docx and store it as a new master version.  Auth (SPIKE — see PRODUCTION NOTE in module docstring):   The caller must supply the bind_token in the X-Bind-Token header.   The token is verified (signature + expiry + scope) to prove the caller   holds the file that was served by /word/open.   PRODUCTION: replace bind-token auth with durable session token +   require_quotes_enabled dependency per spec §9.  Version-fence:   base_version must equal the current head version_no for this quote\'s   contract.  If not (someone else pushed a new version, or the doc is   stale) → 409 Conflict.  The add-in must reconcile (re-open the new   master) before pushing again.  Validation:   - File must start with PK\\x03\\x04 (OOXML/ZIP magic).   - File size must be <= 30 MiB.  Returns:   200 {doc_key, version_no, status: \"stored\"} on success.   409 {detail: \"Version conflict...\"} on stale base_version.   422 on invalid bytes.   401/403 on bad/missing token.
     * @summary Word Sync
     * @param {string} quoteId 
     * @param {File} file The current .docx from the add-in
     * @param {number} baseVersion The version_no the add-in derived this doc from
     * @param {string} [xBindToken] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WordAddinApiInterface
     */
    wordSync(quoteId: string, file: File, baseVersion: number, xBindToken?: string, options?: RawAxiosRequestConfig): AxiosPromise<any>;

}

/**
 * WordAddinApi - object-oriented interface
 * @export
 * @class WordAddinApi
 * @extends {BaseAPI}
 */
export class WordAddinApi extends BaseAPI implements WordAddinApiInterface {
    /**
     * Serve the latest master .docx stamped with a Moolabs bind custom-XML part.  The bind part is a ZIP-embedded XML element (namespace urn:moolabs:bind) whose text content is a JSON object:   {tenant_id, quote_id, doc_key, base_version, bind_token}  The bind_token is a short-TTL (30 min) HS256 JWT scoped to (tenant_id, quote_id, doc_key) with purpose=\"bind\".  It expires so a forwarded copy of the file cannot be replayed after the TTL.  The custom-XML part is NOT visible in Word\'s standard UI but IS readable by any party who unzips the file — ids only, no long-lived secrets.  Returns 404 when no document has been uploaded for this quote. Returns 503 when WORD_ADDIN_JWT_SECRET is not configured.
     * @summary Word Open
     * @param {string} quoteId 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WordAddinApi
     */
    public wordOpen(quoteId: string, options?: RawAxiosRequestConfig) {
        return WordAddinApiFp(this.configuration).wordOpen(quoteId, options).then((request) => request(this.axios, this.basePath));
    }

    /**
     * Accept the add-in\'s current .docx and store it as a new master version.  Auth (SPIKE — see PRODUCTION NOTE in module docstring):   The caller must supply the bind_token in the X-Bind-Token header.   The token is verified (signature + expiry + scope) to prove the caller   holds the file that was served by /word/open.   PRODUCTION: replace bind-token auth with durable session token +   require_quotes_enabled dependency per spec §9.  Version-fence:   base_version must equal the current head version_no for this quote\'s   contract.  If not (someone else pushed a new version, or the doc is   stale) → 409 Conflict.  The add-in must reconcile (re-open the new   master) before pushing again.  Validation:   - File must start with PK\\x03\\x04 (OOXML/ZIP magic).   - File size must be <= 30 MiB.  Returns:   200 {doc_key, version_no, status: \"stored\"} on success.   409 {detail: \"Version conflict...\"} on stale base_version.   422 on invalid bytes.   401/403 on bad/missing token.
     * @summary Word Sync
     * @param {string} quoteId 
     * @param {File} file The current .docx from the add-in
     * @param {number} baseVersion The version_no the add-in derived this doc from
     * @param {string} [xBindToken] 
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WordAddinApi
     */
    public wordSync(quoteId: string, file: File, baseVersion: number, xBindToken?: string, options?: RawAxiosRequestConfig) {
        return WordAddinApiFp(this.configuration).wordSync(quoteId, file, baseVersion, xBindToken, options).then((request) => request(this.axios, this.basePath));
    }
}

