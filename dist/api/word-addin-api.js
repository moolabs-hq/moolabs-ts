"use strict";
/* tslint:disable */
/* eslint-disable */
/**
 * Moolabs API
 * Unified Moolabs SDK. Capability-based public surface stitched from internal service specs: bff, meter, arc, acute. Customers see capability namespaces (usage, billing, collections, etc.); the SDK routes per-operation internally.
 *
 * The version of the OpenAPI document: 0.3.0-rc2
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WordAddinApi = exports.WordAddinApiFactory = exports.WordAddinApiFp = exports.WordAddinApiAxiosParamCreator = void 0;
const axios_1 = require("axios");
// Some imports not used depending on template conditions
// @ts-ignore
const common_1 = require("../common");
// @ts-ignore
const base_1 = require("../base");
/**
 * WordAddinApi - axios parameter creator
 * @export
 */
const WordAddinApiAxiosParamCreator = function (configuration) {
    return {
        /**
         * Serve the latest master .docx stamped with a Moolabs bind custom-XML part.  The bind part is a ZIP-embedded XML element (namespace urn:moolabs:bind) whose text content is a JSON object:   {tenant_id, quote_id, doc_key, base_version, bind_token}  The bind_token is a short-TTL (30 min) HS256 JWT scoped to (tenant_id, quote_id, doc_key) with purpose=\"bind\".  It expires so a forwarded copy of the file cannot be replayed after the TTL.  The custom-XML part is NOT visible in Word\'s standard UI but IS readable by any party who unzips the file — ids only, no long-lived secrets.  Returns 404 when no document has been uploaded for this quote. Returns 503 when WORD_ADDIN_JWT_SECRET is not configured.
         * @summary Word Open
         * @param {string} quoteId
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        wordOpen: (quoteId_1, ...args_1) => __awaiter(this, [quoteId_1, ...args_1], void 0, function* (quoteId, options = {}) {
            // verify required parameter 'quoteId' is not null or undefined
            (0, common_1.assertParamExists)('wordOpen', 'quoteId', quoteId);
            const localVarPath = `/v1/quotes/{quote_id}/contract/editor/word/open`
                .replace(`{${"quote_id"}}`, encodeURIComponent(String(quoteId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, common_1.DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }
            const localVarRequestOptions = Object.assign(Object.assign({ method: 'GET' }, baseOptions), options);
            const localVarHeaderParameter = {};
            const localVarQueryParameter = {};
            // authentication HTTPBearer required
            // http bearer authentication required
            yield (0, common_1.setBearerAuthToObject)(localVarHeaderParameter, configuration);
            (0, common_1.setSearchParams)(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = Object.assign(Object.assign(Object.assign({}, localVarHeaderParameter), headersFromBaseOptions), options.headers);
            return {
                url: (0, common_1.toPathString)(localVarUrlObj),
                options: localVarRequestOptions,
            };
        }),
        /**
         * Accept the add-in\'s current .docx and store it as a new master version.  Auth (SPIKE — see PRODUCTION NOTE in module docstring):   The caller must supply the bind_token in the X-Bind-Token header.   The token is verified (signature + expiry + scope) to prove the caller   holds the file that was served by /word/open.   PRODUCTION: replace bind-token auth with durable session token +   require_quotes_enabled dependency per spec §9.  Version-fence:   base_version must equal the current head version_no for this quote\'s   contract.  If not (someone else pushed a new version, or the doc is   stale) → 409 Conflict.  The add-in must reconcile (re-open the new   master) before pushing again.  Validation:   - File must start with PK\\x03\\x04 (OOXML/ZIP magic).   - File size must be <= 30 MiB.  Returns:   200 {doc_key, version_no, status: \"stored\"} on success.   409 {detail: \"Version conflict...\"} on stale base_version.   422 on invalid bytes.   401/403 on bad/missing token.
         * @summary Word Sync
         * @param {string} quoteId
         * @param {File} file The current .docx from the add-in
         * @param {number} baseVersion The version_no the add-in derived this doc from
         * @param {string} [xBindToken]
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        wordSync: (quoteId_1, file_1, baseVersion_1, xBindToken_1, ...args_1) => __awaiter(this, [quoteId_1, file_1, baseVersion_1, xBindToken_1, ...args_1], void 0, function* (quoteId, file, baseVersion, xBindToken, options = {}) {
            // verify required parameter 'quoteId' is not null or undefined
            (0, common_1.assertParamExists)('wordSync', 'quoteId', quoteId);
            // verify required parameter 'file' is not null or undefined
            (0, common_1.assertParamExists)('wordSync', 'file', file);
            // verify required parameter 'baseVersion' is not null or undefined
            (0, common_1.assertParamExists)('wordSync', 'baseVersion', baseVersion);
            const localVarPath = `/v1/quotes/{quote_id}/contract/editor/word/sync`
                .replace(`{${"quote_id"}}`, encodeURIComponent(String(quoteId)));
            // use dummy base URL string because the URL constructor only accepts absolute URLs.
            const localVarUrlObj = new URL(localVarPath, common_1.DUMMY_BASE_URL);
            let baseOptions;
            if (configuration) {
                baseOptions = configuration.baseOptions;
            }
            const localVarRequestOptions = Object.assign(Object.assign({ method: 'POST' }, baseOptions), options);
            const localVarHeaderParameter = {};
            const localVarQueryParameter = {};
            const localVarFormParams = new ((configuration && configuration.formDataCtor) || FormData)();
            if (file !== undefined) {
                localVarFormParams.append('file', file);
            }
            if (baseVersion !== undefined) {
                localVarFormParams.append('base_version', baseVersion);
            }
            localVarHeaderParameter['Content-Type'] = 'multipart/form-data';
            if (xBindToken != null) {
                localVarHeaderParameter['X-Bind-Token'] = String(xBindToken);
            }
            (0, common_1.setSearchParams)(localVarUrlObj, localVarQueryParameter);
            let headersFromBaseOptions = baseOptions && baseOptions.headers ? baseOptions.headers : {};
            localVarRequestOptions.headers = Object.assign(Object.assign(Object.assign({}, localVarHeaderParameter), headersFromBaseOptions), options.headers);
            localVarRequestOptions.data = localVarFormParams;
            return {
                url: (0, common_1.toPathString)(localVarUrlObj),
                options: localVarRequestOptions,
            };
        }),
    };
};
exports.WordAddinApiAxiosParamCreator = WordAddinApiAxiosParamCreator;
/**
 * WordAddinApi - functional programming interface
 * @export
 */
const WordAddinApiFp = function (configuration) {
    const localVarAxiosParamCreator = (0, exports.WordAddinApiAxiosParamCreator)(configuration);
    return {
        /**
         * Serve the latest master .docx stamped with a Moolabs bind custom-XML part.  The bind part is a ZIP-embedded XML element (namespace urn:moolabs:bind) whose text content is a JSON object:   {tenant_id, quote_id, doc_key, base_version, bind_token}  The bind_token is a short-TTL (30 min) HS256 JWT scoped to (tenant_id, quote_id, doc_key) with purpose=\"bind\".  It expires so a forwarded copy of the file cannot be replayed after the TTL.  The custom-XML part is NOT visible in Word\'s standard UI but IS readable by any party who unzips the file — ids only, no long-lived secrets.  Returns 404 when no document has been uploaded for this quote. Returns 503 when WORD_ADDIN_JWT_SECRET is not configured.
         * @summary Word Open
         * @param {string} quoteId
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        wordOpen(quoteId, options) {
            return __awaiter(this, void 0, void 0, function* () {
                var _a, _b, _c;
                const localVarAxiosArgs = yield localVarAxiosParamCreator.wordOpen(quoteId, options);
                const localVarOperationServerIndex = (_a = configuration === null || configuration === void 0 ? void 0 : configuration.serverIndex) !== null && _a !== void 0 ? _a : 0;
                const localVarOperationServerBasePath = (_c = (_b = base_1.operationServerMap['WordAddinApi.wordOpen']) === null || _b === void 0 ? void 0 : _b[localVarOperationServerIndex]) === null || _c === void 0 ? void 0 : _c.url;
                return (axios, basePath) => (0, common_1.createRequestFunction)(localVarAxiosArgs, axios_1.default, base_1.BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
            });
        },
        /**
         * Accept the add-in\'s current .docx and store it as a new master version.  Auth (SPIKE — see PRODUCTION NOTE in module docstring):   The caller must supply the bind_token in the X-Bind-Token header.   The token is verified (signature + expiry + scope) to prove the caller   holds the file that was served by /word/open.   PRODUCTION: replace bind-token auth with durable session token +   require_quotes_enabled dependency per spec §9.  Version-fence:   base_version must equal the current head version_no for this quote\'s   contract.  If not (someone else pushed a new version, or the doc is   stale) → 409 Conflict.  The add-in must reconcile (re-open the new   master) before pushing again.  Validation:   - File must start with PK\\x03\\x04 (OOXML/ZIP magic).   - File size must be <= 30 MiB.  Returns:   200 {doc_key, version_no, status: \"stored\"} on success.   409 {detail: \"Version conflict...\"} on stale base_version.   422 on invalid bytes.   401/403 on bad/missing token.
         * @summary Word Sync
         * @param {string} quoteId
         * @param {File} file The current .docx from the add-in
         * @param {number} baseVersion The version_no the add-in derived this doc from
         * @param {string} [xBindToken]
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        wordSync(quoteId, file, baseVersion, xBindToken, options) {
            return __awaiter(this, void 0, void 0, function* () {
                var _a, _b, _c;
                const localVarAxiosArgs = yield localVarAxiosParamCreator.wordSync(quoteId, file, baseVersion, xBindToken, options);
                const localVarOperationServerIndex = (_a = configuration === null || configuration === void 0 ? void 0 : configuration.serverIndex) !== null && _a !== void 0 ? _a : 0;
                const localVarOperationServerBasePath = (_c = (_b = base_1.operationServerMap['WordAddinApi.wordSync']) === null || _b === void 0 ? void 0 : _b[localVarOperationServerIndex]) === null || _c === void 0 ? void 0 : _c.url;
                return (axios, basePath) => (0, common_1.createRequestFunction)(localVarAxiosArgs, axios_1.default, base_1.BASE_PATH, configuration)(axios, localVarOperationServerBasePath || basePath);
            });
        },
    };
};
exports.WordAddinApiFp = WordAddinApiFp;
/**
 * WordAddinApi - factory interface
 * @export
 */
const WordAddinApiFactory = function (configuration, basePath, axios) {
    const localVarFp = (0, exports.WordAddinApiFp)(configuration);
    return {
        /**
         * Serve the latest master .docx stamped with a Moolabs bind custom-XML part.  The bind part is a ZIP-embedded XML element (namespace urn:moolabs:bind) whose text content is a JSON object:   {tenant_id, quote_id, doc_key, base_version, bind_token}  The bind_token is a short-TTL (30 min) HS256 JWT scoped to (tenant_id, quote_id, doc_key) with purpose=\"bind\".  It expires so a forwarded copy of the file cannot be replayed after the TTL.  The custom-XML part is NOT visible in Word\'s standard UI but IS readable by any party who unzips the file — ids only, no long-lived secrets.  Returns 404 when no document has been uploaded for this quote. Returns 503 when WORD_ADDIN_JWT_SECRET is not configured.
         * @summary Word Open
         * @param {string} quoteId
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        wordOpen(quoteId, options) {
            return localVarFp.wordOpen(quoteId, options).then((request) => request(axios, basePath));
        },
        /**
         * Accept the add-in\'s current .docx and store it as a new master version.  Auth (SPIKE — see PRODUCTION NOTE in module docstring):   The caller must supply the bind_token in the X-Bind-Token header.   The token is verified (signature + expiry + scope) to prove the caller   holds the file that was served by /word/open.   PRODUCTION: replace bind-token auth with durable session token +   require_quotes_enabled dependency per spec §9.  Version-fence:   base_version must equal the current head version_no for this quote\'s   contract.  If not (someone else pushed a new version, or the doc is   stale) → 409 Conflict.  The add-in must reconcile (re-open the new   master) before pushing again.  Validation:   - File must start with PK\\x03\\x04 (OOXML/ZIP magic).   - File size must be <= 30 MiB.  Returns:   200 {doc_key, version_no, status: \"stored\"} on success.   409 {detail: \"Version conflict...\"} on stale base_version.   422 on invalid bytes.   401/403 on bad/missing token.
         * @summary Word Sync
         * @param {string} quoteId
         * @param {File} file The current .docx from the add-in
         * @param {number} baseVersion The version_no the add-in derived this doc from
         * @param {string} [xBindToken]
         * @param {*} [options] Override http request option.
         * @throws {RequiredError}
         */
        wordSync(quoteId, file, baseVersion, xBindToken, options) {
            return localVarFp.wordSync(quoteId, file, baseVersion, xBindToken, options).then((request) => request(axios, basePath));
        },
    };
};
exports.WordAddinApiFactory = WordAddinApiFactory;
/**
 * WordAddinApi - object-oriented interface
 * @export
 * @class WordAddinApi
 * @extends {BaseAPI}
 */
class WordAddinApi extends base_1.BaseAPI {
    /**
     * Serve the latest master .docx stamped with a Moolabs bind custom-XML part.  The bind part is a ZIP-embedded XML element (namespace urn:moolabs:bind) whose text content is a JSON object:   {tenant_id, quote_id, doc_key, base_version, bind_token}  The bind_token is a short-TTL (30 min) HS256 JWT scoped to (tenant_id, quote_id, doc_key) with purpose=\"bind\".  It expires so a forwarded copy of the file cannot be replayed after the TTL.  The custom-XML part is NOT visible in Word\'s standard UI but IS readable by any party who unzips the file — ids only, no long-lived secrets.  Returns 404 when no document has been uploaded for this quote. Returns 503 when WORD_ADDIN_JWT_SECRET is not configured.
     * @summary Word Open
     * @param {string} quoteId
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WordAddinApi
     */
    wordOpen(quoteId, options) {
        return (0, exports.WordAddinApiFp)(this.configuration).wordOpen(quoteId, options).then((request) => request(this.axios, this.basePath));
    }
    /**
     * Accept the add-in\'s current .docx and store it as a new master version.  Auth (SPIKE — see PRODUCTION NOTE in module docstring):   The caller must supply the bind_token in the X-Bind-Token header.   The token is verified (signature + expiry + scope) to prove the caller   holds the file that was served by /word/open.   PRODUCTION: replace bind-token auth with durable session token +   require_quotes_enabled dependency per spec §9.  Version-fence:   base_version must equal the current head version_no for this quote\'s   contract.  If not (someone else pushed a new version, or the doc is   stale) → 409 Conflict.  The add-in must reconcile (re-open the new   master) before pushing again.  Validation:   - File must start with PK\\x03\\x04 (OOXML/ZIP magic).   - File size must be <= 30 MiB.  Returns:   200 {doc_key, version_no, status: \"stored\"} on success.   409 {detail: \"Version conflict...\"} on stale base_version.   422 on invalid bytes.   401/403 on bad/missing token.
     * @summary Word Sync
     * @param {string} quoteId
     * @param {File} file The current .docx from the add-in
     * @param {number} baseVersion The version_no the add-in derived this doc from
     * @param {string} [xBindToken]
     * @param {*} [options] Override http request option.
     * @throws {RequiredError}
     * @memberof WordAddinApi
     */
    wordSync(quoteId, file, baseVersion, xBindToken, options) {
        return (0, exports.WordAddinApiFp)(this.configuration).wordSync(quoteId, file, baseVersion, xBindToken, options).then((request) => request(this.axios, this.basePath));
    }
}
exports.WordAddinApi = WordAddinApi;
